{
  "name": "tradedesk-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute tradedesk-db --local --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute tradedesk-db --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.3",
    "zod": "^3.23.8",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241022.0",
    "@types/bcryptjs": "^2.4.6",
    "typescript": "^5.6.3",
    "wrangler": "^3.80.0"
  }
}
